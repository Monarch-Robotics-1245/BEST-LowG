version: "1.0"

python:
  interpreter: /usr/bin/python3  # Path to the Python interpreter

bootstrap: |
  python3 --version

exclude:
  - name: All
    paths:
      - program.py
      - vex.py
      - AUTOS.md
      - README.md
      - CONSTANTS.md
  - name: PyPep8NamingInspection